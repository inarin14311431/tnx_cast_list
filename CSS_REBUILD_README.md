# CSS再構築版 — Phase 7 本番統合

> この文書はPhase 7の移行記録です。現在の読込規約と変更手順は `docs/CSS_ARCHITECTURE.md` を正とします。

Phase 7では、隔離検証していた新CSSを通常ページへ統合し、旧CSSと互換用の動的CSS処理を本番経路から撤去しました。

## Pass 2 — 閲覧・編集画面重点復元

- キャスト閲覧画面を正常系MHTML／元プロジェクトの表示仕様と再照合。
- サイバースキャン、固定ナビ、3スタイル、3神業、能力値＋CS、技能2カラム、独立スタイル技能、アウトフィットを新CSSで再実装。
- キャスト編集画面の経験点サイドバー＋右側編集本文の構成を再実装。
- プロフィール、スタイル、能力値、一般技能／社会／コネ、スタイル技能、アウトフィット表を実際の生成DOMに合わせて再実装。
- 旧 `css/`、旧 `theme.js`、JavaScriptによる動的CSS生成は復帰させていません。
- 上記の重点復元契約を `scripts/audit-css-rebuild.mjs` の監査対象へ追加しました。

## Pass 3 — 元版の列幅・技能スート位置を実測値へ固定

- 正常系MHTMLと元プロジェクトの最終CSSを再照合し、閲覧画面の最大幅を1400pxへ復元。
- 一般技能は名称252px＋LV48px、スタイル技能は名称180px＋種別72px＋LV48pxとし、両方の最初のスートを左端から300pxに固定。
- 閲覧画面の4スート列を各48px、スタイル技能表を1364px基準へ復元。
- 1200px未満では技能2カラムを1カラムへ切り替え、固定列幅を潰さない元版のレスポンシブ境界へ復元。
- 編集画面の最終状態を再確認し、経験点サイドバー220px・編集領域最大1600pxへ修正。
- 編集画面の一般技能2列ラッパーを現行DOMに合わせて明示し、通常技能の操作列30px、社会・コネの操作列102pxへ復元。
- 編集画面のスタイル技能は元版の42pxレベル列・34pxスート列・44px参照P列・102px操作列を新CSSの変数として固定。
- 300pxのスート開始位置と主要列幅を自動監査へ追加し、将来の変更でずれた場合は検査を失敗させます。

## Pass 4 — 編集・閲覧画面の細部を元版の最終値へ再照合

- 元版と同じ `html 17px` と `small .72em` を基準文字サイズへ復元。
- 編集画面の▲／▼／×を30px角、ボタン間隔2px、操作列102pxへ固定し、汎用ボタンの42px高が勝つ状態を解消。
- スタイル選択を元版の重点UIへ戻し、セレクト最小高56px・強調文字1.18rem・指定マーク欄78pxと選択状態の強調を新CSSで再実装。
- 編集画面の能力値を4列＋全幅CSへ戻し、能力値マトリクスの44px見出し＋2値列も復元。
- 編集プロフィールを3パネル構成へ戻し、「背景設定」欄とキャスト画像のプレビュー／スキャン／ドロップ領域を新CSSへ再実装。
- スタイル技能・アウトフィットの解説欄は横スクロールを禁止し、複数行を縦方向へ折り返す仕様へ統一。
- 閲覧画面のパーソナルデータを2列（左3項目／右4項目）へ復元。
- 閲覧アウトフィットを元版の列幅へ戻し、分類92px・名称150px・解説320pxとカテゴリ別最小表幅を固定。解説は全表示時にも横へ伸ばさず縦展開。
- 閲覧画面のARCHIVE ID、戻るリンク、折りたたみ記号、能力値、コンパクトヒーローの最終表示仕様を再実装。
- 旧 `theme.js` が暗黙に読み込んでいたOFC列・取込互換などの機能処理は、CSS読込と分離して `sheet.html` から必要スクリプトだけを明示読込する構成へ変更。
- 上記の寸法・プロフィール2列・アウトフィット列・横スクロール禁止・機能スクリプト明示読込を自動監査へ追加。

## Pass 5 — 操作部品・技能配置・共通ヘッダーの実画面差分を修正

- ペルソナ◎／キー●は同じ24pxの描画枠と20pxの外径で描画し、グリフ差による見かけの大小を解消。
- 編集画面のCSを4能力値の右側へ配置し、5枚を同一行で比較できる構成へ変更。
- 一般技能／社会／コネの追加操作を各グループ見出し右上へ移し、元の上部3ボタンを非表示にして重複を解消。
- スタイル技能は内側の重複タイトル／追加操作を撤去し、追加・区切り・TSV取込・検索をセクション直下の主ツールバーへ集約。
- ▲／▼／×は30px角・2px間隔を維持しつつ操作列を106pxへ拡張。スタイル技能では解説列の比率を削り、左端ボタンが欠けない幅を確保。
- 編集画面の「キャストを閲覧」は新規タブを廃止し、同一ウィンドウで遷移。
- 閲覧画面の一般技能を2列化。左列は名称252px＋LV48pxの300px地点から48pxスート列を開始し、スタイル技能の名称180px＋種別72px＋LV48pxと同じスート中心線を維持。
- 閲覧側で不要な詳細列はセルだけでなく `colgroup` の予約幅も除去し、固定列位置のずれを防止。
- 閲覧画面のPROFILE／OUTFITSタブはテーマアクセントを基準に非選択／選択状態のコントラストを復元。
- 閲覧ヘッダーの `N◎VA MUNICIPAL DATABASE` を撤去し、戻り先を「キャスト一覧 / RETURN TO ARCHIVE」へ統一。
- account／acts／backup／cast／combos／login／sheet／showcase-generator の戻る操作に共通 `app-back-link` を適用し、元版の枠なし二段リンクへ統一。
- 上記の最終状態を `scripts/audit-css-rebuild.mjs` の監査条件へ追加。

## Pass 6 — 周辺画面・入力視認性・旧JSON取込を復元

- indexの可視タイトルを `CAST ARCHIVE` のみに整理し、改行を禁止。`N◎VA MUNICIPAL DATABASE` も拡大。
- 単行入力欄は上下paddingを0にしてコントロール高の中央へ文字を配置し、技能・スタイル技能・アウトフィットの文字を列幅を壊さない範囲で拡大。
- indexのキャスト画像を260px固定枠＋`object-fit: cover`＋上端基準へ固定し、アクト紹介の選択画像も72×78px／70×90pxの元寸法へ復元。公開アクト側も上端基準のトリミングへ統一。
- accountを元版の960px幅、縦型ユーザー情報、コンパクトな登録キャスト行へ戻し、6つの主要操作はPass 5の3列配置を維持。
- accountの `SKD・OFC検索マスタ` 管理領域と、編集画面内のSKD/OFC検索ダイアログ／ツールバーの専用デザインを新CSSへ復元。
- アクト紹介生成を元版の1440pxレイアウト、4セクション色分け、キャスト選択カード、出演順編集、非公開／手動キャスト、生成操作まで新CSSへ復元。
- 一般技能JSON取込が2列ラッパー配下を検索できない不具合を修正し、製作／芸術／操縦の専門技能スロットも既存行を優先して取り込むよう修正。
- データ取込中は「閉じる」をdisabledにし、Escによるダイアログ終了も抑止。完了／エラー後にのみ再有効化。
- 進捗率を基本取込0〜50%、アウトフィット最終変換52〜98%、完了100%へ再配分し、最終変換中も列生成時と1件反映時の2段階で進むよう変更。
- 配置先URLを `/tnx_cast_list/` 固定だけにせず実行ページから解決し、コンボ／参加アクトなどの保護画面リンクをサブディレクトリ配布でも正しく遷移させるよう修正。
- 複数の保護画面モジュールが同時にセッション確認する場合は同一の進行中リクエストを共有し、Supabase認証ロックの競合を抑制。
- 上記をCSS再構築監査へ追加し、旧CSS・旧 `theme.js`・実行時CSS生成0件の条件を維持。

## 再構築の原則

旧CSSと旧 `theme.js` は、元の表示仕様を確認するための参照資料としてのみ扱います。本番表示には読み込みません。
必要なデザインと演出は、現在のDOMの責務を整理したうえで `css-next` に再実装します。
JavaScriptは状態・データ・実行時数値を担当し、CSSファイルの後読みや `style` 要素生成による見た目補正には使用しません。

今回の復元ゲートでは、indexの認証ナビ、castのロードスキャン／追従ナビ／スタイル／神業／能力値+CS／技能配置、sheetの本文+経験点サイドバー、account/actsの上部ヘッダーを新CSSの必須仕様として検査します。

## Phase 7での変更

- テーマ対応20画面を `css-next/pages/*-entry.css` 経由の読込へ統一
- `css-next/index.css` を全画面共通の基礎部品だけに限定
- `/next/` を介する画面遷移を廃止
- プレビュー専用の `<base>`、リンク補正、CSSガードを本番HTMLから撤去
- 旧 `theme.js` を本番構成から撤去
- 旧 `css/` ディレクトリを配布物から撤去
- JavaScriptによるCSS後読み・`<style>`生成を0件へ整理
- ココフォリア、ユドナリウム、転記TSV、検索ダイアログ、取込進捗、HELP、CS表示の装飾を新CSSへ固定
- アクト紹介HTMLの追加装飾を生成時CSSへ統合
- 旧アクト公開ページ専用CSSは独立資産として `assets/styles/act-showcase.css` へ移動
- 画面判定をプレビュー専用属性から `data-page` へ変更
- HTMLのプレビュー専用属性と通常画面のインラインstyleを撤去

## 維持している修正

- Supabaseクライアントのローカル同梱
- 認証確認と公開キャスト取得の独立実行
- 認証確認の5秒タイムアウト
- Supabase認証ロックと競合する処理の回避
- 旧版由来の16テーマ名・配色
- 「日本」選択時の「日本国電脳鎖国結界／不法接続」全画面警告
- 狭幅画面での表スクロール、コンボ、バックアップ画面の配置

## 通常URLでの確認

プロジェクトのルートフォルダで次を実行します。

```bat
node scripts\audit-css-rebuild.mjs
npx http-server . -p 8080 -c-1
```

主な確認先：

```text
http://localhost:8080/index.html
http://localhost:8080/cast.html?id=TNX-000060
http://localhost:8080/sheet.html?id=TNX-000060
http://localhost:8080/account.html
http://localhost:8080/backup.html
http://localhost:8080/404.html
```

Phase 7以降は `/next/index.html` ではなく、上記の通常URLが本番確認先です。

## 自動検査

`node scripts\audit-css-rebuild.mjs` は次を検査します。

- `css-next` 配下の全CSS
- テーマ対応20画面の専用エントリ読込
- 旧CSS参照なし
- 旧 `theme.js` 参照なし
- `/next/` 依存なし
- プレビュー専用属性なし
- 新CSS内の `!important` 0件
- 新CSS内の重複セレクタ 0件
- JavaScriptによるCSS生成・後読み 0件
- 16テーマの主要変数
- 日本テーマ警告画面のDOM・文言・専用スタイル
- Supabaseローカルモジュールと初期化保護

## 表示確認項目

- 一覧が通常URLでSupabaseから読み込まれる
- 一覧から `/cast.html?id=...` へ遷移する
- 個別キャストの「編集」から `/sheet.html?id=...` へ遷移する
- 編集画面の保存・取込・技能・アウトフィット操作が利用できる
- 16テーマが切り替わり、再読込後も選択状態が維持される
- 「日本」を選ぶと不法接続警告へ切り替わる
- 警告画面のテーマ選択から通常テーマへ復帰できる
- バックアップ／404／ログイン／アカウントが新CSSで表示される
- 狭幅時も主要操作へ到達でき、横長表は横スクロールできる

Chromeに古いCSSが残る場合は `Ctrl + Shift + R` で強制再読み込みしてください。
