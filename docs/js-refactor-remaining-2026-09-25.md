# JavaScript整理：残作業と完了条件（2026-09-25）

## 変更範囲
検証環境のみ。本番同期は別のリリース工程とする。
DB、保存ペイロード、OFC/SKD、CSS、演出タイミングは変更しない。
既存の旧データ読み取り互換は削除しない。vendorは分割対象外。

## 完了した境界
- PR #431: キャスト演出の初期起動イベント化。タイマー維持。
- PR #432: シート操作ボタンのバインドを分離。
- PR #433: 読込時の画面適用と属性表示を分離。
- PR #434: 神業・能力値・CSの表示DOM分離。3描画モジュールの同一HTMLエスケープを共通化。
- この変更: cast.jsのコンボ値整形・技能名・使用上限・種別判定をDOM非依存モジュールへ分離。
  カウンター保存・クリック処理・印刷からの利用は維持。

## 2026-09-26の整理
- `cast-quick-sheet-html.js`: 簡易印刷のHTML生成、技能・装備表、継続ページのヘッダー／フッターを所有。
  `cast.js`は使用回数のlocalStorage読込み結果をMapとして渡し、DOM適用・画像失敗処理・描画イベント・ページ分割・密度調整を所有する。
- `cast-display-format.js`: 閲覧と簡易印刷で同じ値・ハンドル表記を共有。
  コンボ能力値ラベルは既存の`cast-combo-rules.js`へ集約。
- `showcase-output-html.js`: スタンドアロンHTML生成と選択UI共用の書式処理を所有。
  公開キャストURLの基準URLは呼出側が明示的に渡す。
  `showcase-generator-v3.js`の認証・取得・選択・公開可否・保存処理は維持。
  出力CSSはPR #436で分離済みの`showcase-output-css.js`を引き続き使用。
- 変更前`54a5357`から取得したHTMLのSHA256に対し、空欄／長文／旧形式／装備各種／コンボ使用回数と
  4テーマ＋不明テーマ×背景有無（公開・非公開・手動キャスト混在）の計13ケースを完全一致確認。
- 参照監査: 260 JSファイルすべて到達可能、389 import辺、循環警告0件。不要ファイルの削除なし。
  今回の抽出で共有処理の複製は行っていない。castとshowcaseのHTMLエスケープはアポストロフィの出力が
  `&#39;`と`&#039;`で異なるため、完全一致を維持する本変更では統合しない。
- 合成データのみを使うブラウザ回帰`output-rendering.spec.js`をci-publicへ追加。
  簡易印刷の解説切替・継続ページ・再表示と、紹介生成UIの公開／非公開制御を確認する。

## リリース時に残る確認
1. PRの5ゲートと、マージ後mainの5ゲート・Pages成功。
2. 本番未反映runtimeの整理。本番同期は別リリース工程で、検証専用テスト構成は同期しない。

## 各変更の合格条件
- 計算ルールはDOM非依存。DOM適用には既存の計算結果を渡す。
- 単なる関数の文字列存在検査だけでなく、実際の出力・境界値を検証する。
- Regression checks / Quality gates / Security audit / Playwright E2E / Visual Regression が全成功。
- マージ後のmain CIとPagesデプロイも確認する。
- 画面差分の期待値を、リファクタリングを通す目的で更新しない。

上記は今回の整理範囲の完了条件であり、全ファイルの再設計や旧データ互換処理の削除を意味しない。
