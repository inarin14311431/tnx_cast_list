/* Obsolete compatibility shim.
 * General-skill slot handling is now centralized in general-master-skills.js.
 * Kept as an empty file because older cached sheet.html versions may still
 * request this path. */
(()=>{})();